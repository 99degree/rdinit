# rdinit
Rdinit container is a very init container manager and loader for unmodified Android. The rdinit named for kernel to get in userland the first task is either rdinit or init that run in pid 1. This is the rdinit to create a new name space for unmodified android to run as a container
